# HyperNodes Benchmark suite

This is a very crude suite atm.  
Feel free to suggest new benchmarks

## Requirements

Uses pytest and pytest-benchmark.  
`pip install -r requirements.txt`


## How to run

run with  
`pytest`